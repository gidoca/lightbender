[package]
name = "lightbender-scene"
version = "0.1.0"
edition = "2024"

[dependencies]
glam     = { version = "0.29", features = ["serde"] }
bytemuck = { version = "1", features = ["derive"] }
serde    = { version = "1", features = ["derive"] }
